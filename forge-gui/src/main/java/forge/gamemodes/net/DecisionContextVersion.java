package forge.gamemodes.net;

/** Shared schema marker for external decision-context consumers. */
public final class DecisionContextVersion {
    private DecisionContextVersion() {}
    public static final int CURRENT = DecisionContext.SCHEMA_VERSION;
}
